# Terraform working files — never commit state (it can hold the cluster CA and
# other sensitive values) or downloaded providers.
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
